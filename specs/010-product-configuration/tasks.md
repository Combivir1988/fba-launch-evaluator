# Tasks: этап 2 — конфигурация продукта и ТЗ производителю (spec 010)

Порядок — снизу вверх по зависимостям: ядро → сервер → интерфейс → пробы → выпуск. Тесты пишутся вместе с модулем.

## Фаза 1 — ядро (`shared/`, чисто, без сети)

- [x] T001 `shared/thresholds.js`: группа `config` (D11) + подписи в `THR_NAMES`/`THR_LABELS`; `shared/analysis.js`: `config: null` в новом документе, `configDone` в `metaFromCore`
- [x] T002 `shared/config-scope.js`: `configScope(analysis, th)` → область ASIN (D4); тест на дубли, исключённые бренды, порог 150
- [x] T003 `shared/config-extract.js`: `normalizeValue(field, raw)`, `mergeExtraction(schema, prevTable, aiItems, listings)` с сохранением `manual` и покрытием (D6); `renameOption(schema, table, fieldId, from, to)`; тесты
- [x] T004 `shared/config-stats.js`: `configStats(schema, table, xray, { band })` (D7): доли, «нет данных», премия, доминанта, числовые интервалы; инвариант суммы 100 %; тест на фикстуре; подключение в `compute()` → `results.config`; `stripNew` в `tests/helpers/results-hash.js` (отпечатки без изменений)
- [x] T005 `shared/tz-payload.js`: `buildTzPayload(analysis)` + `collectTzNumbers(payload)` для постпроверки чисел (D9); `share-snapshot.js`: удаление `config.tz` и `aggregates.listings` (D10); тесты

## Фаза 2 — сервер

- [x] T006 `server/scrapfly.js`: `fetchListing(asin, cfg, { fetchImpl, signal })`, `normalizeProduct(data)`, `fetchMany(asins, …, { concurrency: 3, onProgress })`, классификация ошибок, `mockListing` (D2, D12); фикстуры `tests/fixtures/scrapfly-*.json` из ответов пробы (только `extracted_data.data`, без отзывов/служебных полей); `tests/scrapfly.test.js`
- [x] T007 `server/config-prompts.js` (`FIELDS_SCHEMA`, `EXTRACT_SCHEMA`, `TZ_SCHEMA`, тексты промптов без упоминаний курса), `server/mock-config.js`, `server/config-jobs.js`: `schemaStream`, `extractStream`, `tzStream` (D3, D5, D6, D9) с `fetchImpl`/`aiJson` для тестов; `tests/config-jobs.test.js` (кэш не перезагружается, предел 150, пачки, частичный результат при ошибке кредитов, `unverified` в ТЗ)
- [x] T008 `server/tz-docx.js` (`buildTzDocx(tz, meta)` на пакете `docx`) + `tests/tz-docx.test.js` (распаковка zip-записи `word/document.xml` и проверка текста); `package.json`
- [x] T009 `server/claude.js` (`scrapflyKey`), `server/index.js`: маршруты `POST /api/config/schema|extract|tz`, `POST /api/tz/docx`, `scrapfly` в health, 400 без ключа; `tests/analyses-api.test.js`/`server.test.js` — маршруты и health

## Фаза 3 — интерфейс

- [x] T010 `public/js/render.js`: секции `config` (счётчики, кнопки, переключатель диапазона, сетка doughnut-диаграмм, доминирующая конфигурация, таблица ASIN × поля в `<details>`, незагруженные) и `tz` (таблица с правкой, DOCX); подсказки в `TIPS`/`KEY_TIPS`; `tests/render.test.js` + `tips.test.js`
- [x] T011 `public/js/ai.js` (`runConfigSchema/Extract/Tz`, `pendingJob` типы), `public/js/app.js` (запуск задач с прогрессом, слияние `listings`, диалог `#schema-dlg`, правки клеток и строк ТЗ → `markDirty`, «Скачать DOCX» через `fetch` + `download`), `public/index.html` (диалог, справка «Этап 2»), `public/css/app.css`
- [x] T012 `scripts/ui-probe17.mjs` (MOCK): Xray → схема → правка схемы → извлечение → диаграммы (сумма 100 %) → переключатель диапазона → правка клетки → ТЗ → DOCX скачан (PK-заголовок) → F5 во время задачи → публичная ссылка без ТЗ

## Фаза 4 — выпуск

- [x] T013 Регрессия (`npm test`, пробы 12–17), grep на «урок»/бренды, коммит, push, `SCRAPFLY_API_KEY` в переменные Railway, деплой при `health.running = 0`, живая проверка на анализе «Amaranthus» (≤ 20 ASIN — контроль кредитов), память проекта и внешний CLAUDE.md

## Выпуск

- 2026-09-22: коммиты 2642724 (ядро) · 5bdc972 (сервер) · 75b4713 (интерфейс) · 5458fee (повторы AI, partial) — на проде, `SCRAPFLY_API_KEY` задан в переменных Railway, `health.scrapfly = true`. Тесты 266, проба `ui-probe17` — 26 проверок, пробы 12/13/16 зелёные.
- Живая проверка на проде («Amaranthus», топ-15 ASIN, ничего в анализ не записано): схема — 8 полей по 15 листингам, 390 кредитов Scrapfly (26 на страницу), 131 с; извлечение по кэшу — 0 кредитов, покрытие 53–100 % (среднее 78 %), значений вне списка 0; ТЗ — 12 строк, непроверенных чисел 0; DOCX 11,7 KB. Первая попытка упала на перегрузке бесплатной модели Google — отсюда повторы AI и partial (5458fee). В топе выручки «Amaranthus» — гортензии: перед этапом 2 стоит исключить чужие бренды (панель, раздел 4).
- Не сделано (по решению владельца): чтение картинок A+/карусели, XLSX, лист поставщиков.
- 2026-09-22 (вечер): владелец не нашёл секции («ніде не бачу» — они стояли на глубине 10 500 px из 16 000). Добавлены раздел 9 панели (634f0cb) и две вкладки над дашбордом «Этап 1 · оценка ниши» / «Этап 2 · конфигурация продукта и ТЗ» с запоминанием выбора; проба ui-probe17 — 34 проверки.
- 2026-09-22 (позже): раздел 9 панели убран по замечанию владельца — дублировал кнопки секции этапа 2; остались вкладки, бейдж состояния на вкладке и ссылка в шапке.
- 2026-09-22 (ночь): вкладки перенесены внутрь render.js — теперь и в публичной ссылке, и в автономном HTML («а то виходить простиня»); у каждого графика подсказка Chart.js с значением и описанием «что это». 268 тестов, ui-probe17 — 35 проверок.
- 2026-09-22 (поздно): «підказок на діаграмах так і немає» — найдена ошибка с первой версии: `Chart.defaults.animation = {duration}` терял тип анимации цвета, наведение бросало исключение и подсказка не рисовалась. Исправлено; кольцо/радар — подсказка ближайшего сектора; значок «?» у графиков; проба ui-probe18 проверяет реальную отрисовку.
- 2026-09-22 (ночь, 2): «при составлении ТЗ — ответ не по схеме, со второй попытки прошло» — enum разделов/приоритета убраны из схемы (модель через json_object их нарушала), нормализация на сервере, один повтор при ошибке разбора, ошибки задач в журнале.
- 2026-09-23: подсказки графиков сокращены до чисел (без пояснений, без дублирующего заголовка кольца); пояснение — только у значка «?».
- 2026-09-23: вне spec 010 — чистка брендовых запросов Cerebro (бренды из Xray+POE+исключённых, апострофы/двухсловные, фраза с брендом выпадает из кластера при загрузке файлов).
- 2026-09-23: вне spec 010 — панель: карточки файлов расширяли панель (горизонтальный скролл, срезанный край CVR); «Деньги по месяцам»: заметка о стартовом уровне продаж (с нуля / новички / вручную) над таблицей.
- 2026-09-23: «в тайтле MERV 13 и 20x25x4, а в таблице нет данных» — схема из топ-15 не содержала этих вариантов, а промпт велел «только из списка, иначе нет данных» (58 строк размера потеряны). Теперь значение вне списка сохраняется дословно с пометкой * (unlisted), считается в диаграммах своей группой; промпт: «нет данных» только когда поля нет в тексте; в диалоге схемы — чипы «вне списка в таблице» с добавлением в список одним кликом. Перезапуск извлечения по кэшу — 0 кредитов.
- 2026-09-23: схема полей — по 40 листингам вместо 15 (те же страницы потом идут в извлечение — лишних кредитов нет), списки значений до 20, промпт просит ВСЕ встретившиеся варианты.
- 2026-09-23: «зараз взагалі все зломалось» — после «Предложить схему заново» модель дала новые id полей, таблица под старыми id → «нет данных» везде, кроме совпавшего merv_rating. Теперь: повторная схема получает текущую схему и сохраняет id/названия существующих полей (расширяя списки); если схема всё же изменилась после извлечения — заметная плашка «Схема изменилась… нажмите «Извлечь заново» (кэш, 0 кредитов)».
