# Tasks: личные наборы порогов (spec 011)

- [x] T001 `shared/thresholds.js`: `thresholdOverrides(custom)` (только известные пороги, правильный тип, только отличия), `sanitizePresets(list)`, `PRESET_LIMITS`; тесты
- [x] T002 `server/users.js`: ключ `thresholdPresets` в `updateSettings` с проверкой; тест в `users.test.js`
- [x] T003 Интерфейс «Пороги»: панель наборов (выбор / загрузить / сохранить как / обновить / удалить), строка «изменено порогов: N»; справка
- [x] T004 `scripts/ui-probe19.mjs`: изменить порог → сохранить набор → «＋ Новый» даёт умолчания → загрузить набор → второй пользователь наборов не видит
- [x] T005 Регрессия, коммит, push, деплой при `health.running = 0`, память проекта и внешний CLAUDE.md

## Выпуск

- 2026-09-23: реализовано и на проде (без миграций). Наборы — в `users.settings.thresholdPresets` (только отличия от умолчаний, ≤ 20); вкладка «Пороги» → «Мои наборы порогов»; новый анализ всегда с умолчаниями. Тесты thresholds-presets + users, проба ui-probe19 — 14 проверок. Попутно: обработчик порогов игнорирует `change` от удалённого при перерисовке поля (Chrome шлёт его при потере фокуса — попадал в уже другой анализ).
