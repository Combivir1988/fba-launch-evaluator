# Tasks: правила сеансов (spec 008)

- [x] T001 Миграция `003_app_settings.sql`; `server/app-settings.js`: чтение с кэшем, запись с проверкой по спискам, «кто и когда менял»
- [x] T002 `server/sessions.js`: проверка `maxDays` и `idleMinutes` в `resolveSession`, активность в памяти с записью в базу не чаще раза в минуту, причина окончания в ответе 401
- [x] T003 `server/index.js`: `GET/PUT /api/admin/session-policy` (admin), `sessionPolicy` в `/api/auth/me`, запись в журнал
- [x] T004 `public/js/idle.js`: активность по всем вкладкам, сверка с сервером, предупреждение за 60 с, сохранение и выход
- [x] T005 Приложение: подключение, диалог «Вы ещё здесь?», блок «Сеансы» в настройках администратора; страница входа объясняет причину; справка
- [x] T006 `tests/session-policy.test.js`: настройки, бездействие (SC-001), запись раз в минуту, максимальная длительность, действие на открытые сеансы, прежнее поведение без правил, API и права
- [x] T007 `scripts/ui-probe15.mjs`: 15 проверок с управляемыми часами браузера
- [x] T008 Регрессия: все тесты, пробы 7, 11, 13
- [x] T009 Коммит, push, деплой при `health.running = 0`, проверка миграции и маршрутов на проде; память проекта и внешний CLAUDE.md

## Выпуск

- 2026-09-22: коммит 1f1e54c на проде, миграция 003 применена на Neon. Тесты 227, проба `ui-probe15` — 15 проверок, пробы 7, 11, 13 зелёные. Проверка на проде через временные сеансы (8 из 8), правило на проде не менялось — выключено, выбирает владелец.
- 2026-09-24: убрано «мигание» приложением при закончившемся сеансе. Раньше `/` отдавалась статикой всем, оболочка успевала отрисоваться и только потом `GET /api/auth/me` → 401 уводил на вход. Теперь главная (`/`, `/index.html`) проверяет cookie на сервере и без действующего сеанса отвечает 302 на `/login.html?next=…` (кэш `no-store`; ошибка хранилища — приложение отдаётся как прежде), а страница до подтверждения сеанса скрыта классом `body.booting` с заставкой «Проверяем сеанс…» — на случай кэша браузера и сеанса, умершего между загрузкой и проверкой. `isLeaving()` в `api.js` отличает переход на вход от сетевой ошибки: при ошибке приложение показывается с объяснением. Проба `ui-probe22` (12 проверок), тест статики в `server.test.js` переписан. На проде правило бездействия администратором включено — 15 минут, поэтому вход и запрашивается так часто.
