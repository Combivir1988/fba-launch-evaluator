# Tasks: ценовой ориентир по конфигурации (spec 013)

- [x] T001 `shared/price-config.js` (`priceByConfig`, `wquantile`) + `tests/price-config.test.js`; `results.priceConfig` в `compute()`; `stripNew`
- [x] T002 Интерфейс: блок «Цена вашей конфигурации» (селекты, карточки, вклады, «Подставить в экономику»), `core.config.priceChoice`; справка, подсказки
- [x] T003 ТЗ: факты `pricing`, раздел «цена и позиционирование»
- [x] T004 Проба `ui-probe17`: блок цены, смена значения меняет аналоги, «Подставить» меняет цену экономики; регрессия; коммит, push, деплой; память и CLAUDE.md

## Выпуск

- 2026-09-23: на проде. `priceByConfig`: аналоги по совпадению выбранных полей (100 % → 80 % → 60 %, минимум `config.minAnalogs` = 5, иначе лучшая ступень с ≥ 3 и предупреждение), взвешенная выручкой медиана / P25 / P75, цена с учётом купонов, вклад полей к доминанте, ориентир себестоимости; выбор в `core.config.priceChoice`; блок «Цена вашей конфигурации» с селектами и «Подставить в экономику»; факты `pricing`/`promo` в ТЗ, раздел «цена и позиционирование». Тесты price-config (4), проба ui-probe17 (38 проверок).
